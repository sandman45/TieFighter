## Uses the account's default VPC/subnet — a single hobby-project
## instance doesn't need dedicated networking.

data "aws_vpc" "default" {
  default = true
}

data "aws_subnets" "default" {
  filter {
    name   = "vpc-id"
    values = [data.aws_vpc.default.id]
  }
}
